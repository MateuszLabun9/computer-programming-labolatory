#include <string>
#include "circle.h"
#include "point.h"
#include <math.h>

using namespace std;


double distance(const Point& p1, const Point& p2)
{
	return sqrt((p1.x - p2.x)*(p1.x - p2.x) + (p1.y - p2.y)*(p1.y - p2.y));
}

double  circlesLocation(const Circle& a, const Circle& b)
{
	/*double abs(double d) =
		if ()*/

}


int main()
{









	system("pause");
}